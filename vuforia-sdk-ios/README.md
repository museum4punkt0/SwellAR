This is where you need to put the Vuforia SDK.

The file structure should end up looking something like this:

	vuforia-sdk-ios/
	  build/
	    include/
	      Vuforia/
	        ... (a bunch of header files)
	  lib/
	    arm/
	      libVuforia.a

